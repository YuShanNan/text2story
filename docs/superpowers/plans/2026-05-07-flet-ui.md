# Flet 桌面 GUI 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 Flet 构建 text2story 桌面 GUI，替代全部 CLI 命令，支持多文件批量流水线处理。

**Architecture:** Flet 顶部 Tab 导航 + 左侧文件面板 + 右侧功能区 + 底部进度日志。async/await 后台执行，复用现有 `core/*` 模块不修改。

**Tech Stack:** Python 3.10+, Flet, asyncio, 现有 core/api/utils 模块

---

### Task 1: 环境准备

**Files:**
- Modify: `requirements.txt`

- [ ] **Step 1: 添加 flet 依赖**

在 `requirements.txt` 末尾追加：
```
flet>=0.25.0
```

- [ ] **Step 2: 安装依赖**

```bash
pip install flet
```

- [ ] **Step 3: 验证安装**

```bash
python -c "import flet; print(flet.__version__)"
```
Expected: 输出版本号，无报错

- [ ] **Step 4: 提交**

```bash
git add requirements.txt
git commit -m "chore: 添加 flet 依赖"
```

---

### Task 2: 入口文件 + 配色主题

**Files:**
- Create: `flet_ui/__init__.py`
- Create: `flet_ui/theme.py`
- Create: `flet_ui/main.py`

- [ ] **Step 1: 创建目录结构**

```bash
mkdir -p E:/text2story/flet_ui/pages E:/text2story/flet_ui/widgets
```

- [ ] **Step 2: 写 `flet_ui/__init__.py`**

```python
# flet_ui package
```

- [ ] **Step 3: 写 `flet_ui/theme.py`**

```python
"""墨蓝黑配色方案"""
import flet as ft

# 颜色常量
BG_PRIMARY = "#020408"
BG_CARD = "#060a14"
BG_INPUT = "#010306"
BORDER = "#182036"
PRIMARY = "#142850"
PRIMARY_LIGHT = "#1a3a68"
TEXT_PRIMARY = "#d0dce8"
TEXT_SECONDARY = "#a0b8d8"
TEXT_MUTED = "#6a80a8"
TEXT_LABEL = "#8898b8"
SUCCESS = "#50c878"
WARNING = "#d8b840"


def get_theme() -> ft.Theme:
    return ft.Theme(
        color_scheme_seed=PRIMARY,
        visual_density=ft.VisualDensity.COMFORTABLE,
    )


def page_style(page: ft.Page):
    """应用全局样式"""
    page.theme = get_theme()
    page.bgcolor = BG_PRIMARY
    page.padding = 0
    page.window.width = 1280
    page.window.height = 800
    page.window.min_width = 960
    page.window.min_height = 600
    page.window.title_bar_hidden = False
    page.title = "text2story"
```

- [ ] **Step 4: 写 `flet_ui/main.py`**

```python
"""Flet 桌面 GUI 入口"""
import flet as ft

from flet_ui.theme import page_style
from flet_ui.pages.pipeline_page import PipelinePage


def main(page: ft.Page):
    page_style(page)

    tabs = ft.Tabs(
        selected_index=0,
        animation_duration=300,
        tabs=[
            ft.Tab(text="流水线", content=PipelinePage(page)),
            ft.Tab(text="SRT 修正", content=ft.Text("SRT 修正 - 开发中")),
            ft.Tab(text="分镜生成", content=ft.Text("分镜生成 - 开发中")),
            ft.Tab(text="提示词生成", content=ft.Text("提示词生成 - 开发中")),
            ft.Tab(text="画面优化", content=ft.Text("画面优化 - 开发中")),
            ft.Tab(text="视频提示词", content=ft.Text("视频提示词 - 开发中")),
        ],
    )

    page.add(tabs)


if __name__ == "__main__":
    ft.app(target=main)
```

- [ ] **Step 5: 验证启动**

```bash
cd E:/text2story && python -c "from flet_ui.theme import BG_PRIMARY, SUCCESS; print(BG_PRIMARY, SUCCESS)"
```
Expected: `#020408 #50c878`

- [ ] **Step 6: 提交**

```bash
git add flet_ui/__init__.py flet_ui/theme.py flet_ui/main.py
git commit -m "feat: Flet 入口 + 墨蓝黑配色主题"
```

---

### Task 3: 左侧文件面板

**Files:**
- Create: `flet_ui/widgets/file_panel.py`

- [ ] **Step 1: 写 `flet_ui/widgets/file_panel.py`**

```python
"""可多选的文件列表面板"""
import os
import flet as ft

from flet_ui.theme import (
    BG_CARD, BG_INPUT, BORDER, PRIMARY, TEXT_PRIMARY,
    TEXT_SECONDARY, TEXT_MUTED, SUCCESS,
)


class FilePanel(ft.UserControl):
    """左侧文件选择面板，支持多选、添加文件/目录"""

    def __init__(self, on_selection_change=None):
        super().__init__()
        self._files: list[str] = []          # 所有发现文件
        self._selected: set[str] = set()     # 已选文件路径
        self._on_selection_change = on_selection_change
        self._file_list = ft.Column(spacing=0)
        self._count_text = ft.Text("已选 0", size=11, color=SUCCESS)

    def build(self):
        return ft.Container(
            width=240,
            bgcolor=BG_CARD,
            border=ft.border.all(1, BORDER),
            border_radius=8,
            padding=14,
            content=ft.Column([
                ft.Row([
                    ft.Text("📁 文件队列", size=11, color=TEXT_SECONDARY),
                    self._count_text,
                ], alignment=ft.MainAxisAlignment.SPACE_BETWEEN),
                ft.Container(
                    bgcolor=BG_INPUT,
                    border=ft.border.all(1, BORDER),
                    border_radius=6,
                    padding=6,
                    content=self._file_list,
                    expand=True,
                ),
                ft.Row([
                    ft.ElevatedButton(
                        "📂 添加文件",
                        style=_btn_style(),
                        on_click=self._pick_files,
                    ),
                    ft.ElevatedButton(
                        "📁 目录",
                        style=_btn_style_secondary(),
                        on_click=self._pick_dir,
                    ),
                ], spacing=6),
                ft.Container(
                    bgcolor=BG_INPUT,
                    border=ft.border.all(1, BORDER),
                    border_radius=6,
                    padding=ft.padding.symmetric(8, 10),
                    content=ft.Text(
                        "输出: output/<文件名>/",
                        size=10,
                        color=TEXT_MUTED,
                    ),
                ),
            ], spacing=10, expand=True),
        )

    @property
    def selected_files(self) -> list[str]:
        return sorted(self._selected)

    def _refresh_list(self):
        self._file_list.controls.clear()
        if not self._files:
            self._file_list.controls.append(
                ft.Text("拖放文件或点击浏览", size=11, color=TEXT_MUTED)
            )
        for path in self._files:
            name = os.path.basename(path)
            size = os.path.getsize(path)
            size_str = f"{size/1024:.1f} KB" if size < 1024*1024 else f"{size/1024/1024:.1f} MB"
            is_selected = path in self._selected
            self._file_list.controls.append(
                ft.Container(
                    content=ft.Row([
                        ft.Icon(ft.icons.CHECK_BOX if is_selected else ft.icons.CHECK_BOX_OUTLINE_BLANK,
                                size=16, color=SUCCESS if is_selected else TEXT_MUTED),
                        ft.Text(name, size=11, color=TEXT_PRIMARY if is_selected else TEXT_MUTED),
                        ft.Text(size_str, size=10, color=TEXT_MUTED),
                    ], spacing=8),
                    on_click=lambda e, p=path: self._toggle(p),
                    padding=ft.padding.symmetric(5, 8),
                    bgcolor=BG_INPUT if is_selected else None,
                    border_radius=3,
                )
            )
        self._count_text.value = f"已选 {len(self._selected)}"
        self._count_text.update()
        self._file_list.update()

    def _toggle(self, path: str):
        if path in self._selected:
            self._selected.discard(path)
        else:
            self._selected.add(path)
        self._refresh_list()
        if self._on_selection_change:
            self._on_selection_change(self._selected)

    def _pick_files(self, e):
        picker = ft.FilePicker(
            on_result=lambda r: self._on_files_picked(r)
        )
        self.page.overlay.append(picker)
        picker.pick_files(allow_multiple=True, file_type="custom",
                          allowed_extensions=["srt", "txt", "csv"])

    def _pick_dir(self, e):
        picker = ft.FilePicker(
            on_result=lambda r: self._on_dir_picked(r)
        )
        self.page.overlay.append(picker)
        picker.get_directory_path()

    def _on_files_picked(self, result):
        if result.files:
            for f in result.files:
                if f.path not in self._files:
                    self._files.append(f.path)
                    self._selected.add(f.path)
            self._refresh_list()
            if self._on_selection_change:
                self._on_selection_change(self._selected)

    def _on_dir_picked(self, result):
        if result.path:
            for name in os.listdir(result.path):
                full = os.path.join(result.path, name)
                if os.path.isfile(full) and name.split(".")[-1] in ("srt", "txt", "csv"):
                    if full not in self._files:
                        self._files.append(full)
                        self._selected.add(full)
            self._refresh_list()
            if self._on_selection_change:
                self._on_selection_change(self._selected)


def _btn_style():
    return ft.ButtonStyle(
        bgcolor=PRIMARY,
        color="#d8e4ff",
        shape=ft.RoundedRectangleBorder(radius=6),
        padding=ft.padding.symmetric(8, 12),
    )


def _btn_style_secondary():
    return ft.ButtonStyle(
        bgcolor=BG_CARD,
        color=TEXT_SECONDARY,
        shape=ft.RoundedRectangleBorder(radius=6),
        side=ft.BorderSide(1, BORDER),
        padding=ft.padding.symmetric(8, 12),
    )
```

- [ ] **Step 2: 验证导入**

```bash
cd E:/text2story && python -c "from flet_ui.widgets.file_panel import FilePanel; print('OK')"
```

- [ ] **Step 3: 提交**

```bash
git add flet_ui/widgets/__init__.py flet_ui/widgets/file_panel.py
git commit -m "feat: 文件面板组件（多选+添加/目录）"
```

---

### Task 4: 底部进度条 + 日志组件

**Files:**
- Create: `flet_ui/widgets/progress_bar.py`
- Create: `flet_ui/widgets/log_viewer.py`

- [ ] **Step 1: 写 `flet_ui/widgets/log_viewer.py`**

```python
"""实时日志流组件"""
import flet as ft
from flet_ui.theme import BG_CARD, BG_INPUT, BORDER, TEXT_PRIMARY, TEXT_MUTED, SUCCESS, WARNING


class LogViewer(ft.UserControl):
    """底部可展开/收起的日志面板"""

    def __init__(self):
        super().__init__()
        self._expanded = False
        self._logs: list[str] = []
        self._log_area = ft.Column(spacing=2, scroll=ft.ScrollMode.ALWAYS)
        self._toggle_text = ft.Text("🔽 展开详细日志", size=10, color=TEXT_MUTED)

    def build(self):
        return ft.Container(
            bgcolor=BG_CARD,
            border=ft.border.all(1, BORDER),
            border_radius=8,
            padding=ft.padding.symmetric(10, 14),
            content=ft.Column([
                self._toggle_text,
                self._log_area,
            ], spacing=6),
            on_click=lambda e: self._toggle(),
        )

    def append(self, text: str, level: str = "info"):
        """添加日志行。level: 'info' | 'warn' | 'success'"""
        color = {"info": TEXT_PRIMARY, "warn": WARNING, "success": SUCCESS}.get(level, TEXT_PRIMARY)
        self._logs.append((text, color))
        if self._expanded:
            self._log_area.controls.append(
                ft.Text(text, size=11, color=color, font_family="monospace")
            )
            self._log_area.update()

    def _toggle(self):
        self._expanded = not self._expanded
        self._toggle_text.value = "🔼 收起日志" if self._expanded else "🔽 展开详细日志"
        self._toggle_text.update()
        if self._expanded:
            self._log_area.controls.clear()
            for text, color in self._logs:
                self._log_area.controls.append(
                    ft.Text(text, size=11, color=color, font_family="monospace")
                )
        else:
            self._log_area.controls.clear()
        self._log_area.update()


class ProgressBar(ft.UserControl):
    """底部进度条"""

    def __init__(self):
        super().__init__()
        self._label = ft.Text("", size=11, color=TEXT_PRIMARY)
        self._pct_text = ft.Text("0%", size=11, color=TEXT_MUTED)
        self._bar = ft.Container(
            bgcolor=BG_INPUT,
            border_radius=3,
            height=5,
            content=ft.Container(
                bgcolor="#1a3a68",
                border_radius=3,
                width=0,
            ),
        )
        self.log_viewer = LogViewer()

    def build(self):
        return ft.Column([
            ft.Container(
                bgcolor=BG_CARD,
                border=ft.border.all(1, BORDER),
                border_radius=8,
                padding=ft.padding.symmetric(10, 14),
                content=ft.Column([
                    ft.Row([
                        self._label,
                        self._pct_text,
                    ], alignment=ft.MainAxisAlignment.SPACE_BETWEEN),
                    self._bar,
                ], spacing=4),
            ),
            self.log_viewer,
        ], spacing=6)

    def update_progress(self, label: str, pct: float):
        """pct: 0-1"""
        self._label.value = label
        self._pct_text.value = f"{int(pct * 100)}%"
        self._bar.content.width = pct
        self._bar.content.update()
        self._label.update()
        self._pct_text.update()

    def log(self, text: str, level: str = "info"):
        self.log_viewer.append(text, level)
```

- [ ] **Step 2: 验证导入**

```bash
cd E:/text2story && python -c "from flet_ui.widgets.progress_bar import ProgressBar; print('OK')"
```

- [ ] **Step 3: 提交**

```bash
git add flet_ui/widgets/progress_bar.py flet_ui/widgets/log_viewer.py
git commit -m "feat: 底部进度条 + 可展开日志组件"
```

---

### Task 5: 规则栏组件

**Files:**
- Create: `flet_ui/widgets/rules_bar.py`

- [ ] **Step 1: 写 `flet_ui/widgets/rules_bar.py`**

```python
"""四规则下拉 + 顺序/并行切换 + 执行按钮（同行）"""
import glob
import os
import flet as ft

from flet_ui.theme import (
    BG_CARD, BG_INPUT, BORDER, PRIMARY, TEXT_PRIMARY, TEXT_SECONDARY, TEXT_LABEL, SUCCESS,
)

PROMPTS_DIR = os.path.join(os.path.dirname(__file__), "..", "..", "prompts")
STEP_MAP = [
    ("SRT修正", "srt_correction"),
    ("分镜生成", "storyboard"),
    ("画面优化", "image_prompt_optimize"),
    ("视频提示词", "video_prompt_from_image"),
]


class RulesBar(ft.UserControl):
    """顶部规则栏：四下拉 + 模式切换 + 执行按钮"""

    def __init__(self, on_execute=None):
        super().__init__()
        self._on_execute = on_execute
        self._sequential = True
        self._dds: list[ft.Dropdown] = []
        self._mode_btn_seq = ft.Container(
            content=ft.Text("顺序", size=10, color=ft.colors.WHITE),
            bgcolor=PRIMARY,
            border_radius=ft.border_radius.all(3),
            padding=ft.padding.symmetric(2, 7),
            on_click=lambda e: self._set_mode(True),
        )
        self._mode_btn_par = ft.Container(
            content=ft.Text("并行", size=10, color=TEXT_SECONDARY),
            border_radius=ft.border_radius.all(3),
            padding=ft.padding.symmetric(2, 7),
            on_click=lambda e: self._set_mode(False),
        )
        self._exec_btn = ft.ElevatedButton(
            "▶ 顺序执行",
            style=_exec_btn_style(),
            on_click=lambda e: self._fire(),
        )
        self._api_text = ft.Text("✓ API已连接", size=10, color=SUCCESS)

    @property
    def sequential(self) -> bool:
        return self._sequential

    @property
    def rules(self) -> dict[str, str]:
        """返回 {step_key: selected_rule}"""
        result = {}
        for i, (_, key) in enumerate(STEP_MAP):
            if i < len(self._dds):
                result[key] = self._dds[i].value or "default"
        return result

    def build(self):
        children = []
        for label, key in STEP_MAP:
            options = _scan_prompts(key)
            dd = ft.Dropdown(
                value="default",
                options=[ft.dropdown.Option(k, k) for k in options],
                width=110,
                text_size=11,
                dense=True,
                bgcolor=BG_INPUT,
                border_color=BORDER,
                color=TEXT_PRIMARY,
                content_padding=ft.padding.symmetric(4, 6),
            )
            self._dds.append(dd)
            children.append(ft.Text(label, size=10, color=TEXT_LABEL))
            children.append(dd)

        children.append(
            ft.Container(
                content=ft.Row([
                    self._mode_btn_seq,
                    self._mode_btn_par,
                ], spacing=0),
                bgcolor=BG_INPUT,
                border=ft.border.all(1, BORDER),
                border_radius=4,
                padding=1,
            )
        )
        children.append(self._api_text)
        children.append(self._exec_btn)

        return ft.Container(
            bgcolor=BG_CARD,
            border=ft.border.all(1, BORDER),
            border_radius=8,
            padding=ft.padding.symmetric(8, 14),
            content=ft.Row(children, spacing=8, scroll=ft.ScrollMode.AUTO),
        )

    def _set_mode(self, seq: bool):
        self._sequential = seq
        bg_seq = PRIMARY if seq else None
        color_seq = ft.colors.WHITE if seq else TEXT_SECONDARY
        bg_par = PRIMARY if not seq else None
        color_par = ft.colors.WHITE if not seq else TEXT_SECONDARY
        self._mode_btn_seq.bgcolor = bg_seq
        self._mode_btn_seq.content.color = color_seq
        self._mode_btn_par.bgcolor = bg_par
        self._mode_btn_par.content.color = color_par
        self._exec_btn.text = "▶ 顺序执行" if seq else "▶ 批量执行"
        self._mode_btn_seq.update()
        self._mode_btn_par.update()
        self._exec_btn.update()

    def _fire(self):
        if self._on_execute:
            self._on_execute()

    def set_api_connected(self, ok: bool):
        self._api_text.value = "✓ API已连接" if ok else "✗ API未连接"
        self._api_text.color = SUCCESS if ok else "#d84040"
        self._api_text.update()


def _scan_prompts(subdir: str) -> list[str]:
    d = os.path.join(PROMPTS_DIR, subdir)
    if not os.path.isdir(d):
        return ["default"]
    files = glob.glob(os.path.join(d, "*.txt"))
    names = [os.path.splitext(os.path.basename(f))[0] for f in files]
    return sorted(names) if names else ["default"]


def _exec_btn_style():
    return ft.ButtonStyle(
        bgcolor=PRIMARY,
        color="#e8f0ff",
        shape=ft.RoundedRectangleBorder(radius=6),
        padding=ft.padding.symmetric(5, 16),
    )
```

- [ ] **Step 2: 验证导入**

```bash
cd E:/text2story && python -c "from flet_ui.widgets.rules_bar import RulesBar; print('OK')"
```

- [ ] **Step 3: 提交**

```bash
git add flet_ui/widgets/rules_bar.py
git commit -m "feat: 规则栏组件（四下拉+模式切换+执行按钮）"
```

---

### Task 6: 执行队列视图

**Files:**
- Create: `flet_ui/widgets/queue_view.py`

- [ ] **Step 1: 写 `flet_ui/widgets/queue_view.py`**

```python
"""执行队列列表（每文件独立进度条）"""
import flet as ft
from flet_ui.theme import BG_CARD, BG_INPUT, BORDER, TEXT_PRIMARY, TEXT_SECONDARY, SUCCESS, WARNING


class QueueView(ft.UserControl):
    """右侧执行队列列表"""

    def __init__(self):
        super().__init__()
        self._items: dict[str, QueueItem] = {}  # filename -> QueueItem
        self._list = ft.Column(spacing=6)

    def build(self):
        return ft.Container(
            bgcolor=BG_CARD,
            border=ft.border.all(1, BORDER),
            border_radius=8,
            padding=ft.padding.symmetric(12, 14),
            content=ft.Column([
                ft.Text("📋 执行队列", size=11, color=TEXT_SECONDARY),
                self._list,
            ], spacing=8),
            expand=True,
        )

    def set_files(self, filenames: list[str]):
        self._items.clear()
        self._list.controls.clear()
        for name in filenames:
            item = QueueItem(name)
            self._items[name] = item
            self._list.controls.append(item)
        self._list.update()

    def set_status(self, filename: str, status: str, pct: float = 1.0):
        """status: 'waiting' | 'running' | 'done' | 'failed'"""
        if filename in self._items:
            self._items[filename].set_status(status, pct)
            self._items[filename].update()


class QueueItem(ft.UserControl):
    def __init__(self, filename: str):
        super().__init__()
        self._name = filename
        self._status = ft.Text("等待", size=11, color=TEXT_SECONDARY)
        self._bar = ft.Container(
            bgcolor=BG_INPUT,
            border_radius=2,
            height=3,
            content=ft.Container(bgcolor="#1a2a50", border_radius=2, width=0),
        )

    def build(self):
        return ft.Column([
            ft.Row([
                ft.Text(self._name, size=11, color=TEXT_PRIMARY),
                self._status,
            ], alignment=ft.MainAxisAlignment.SPACE_BETWEEN),
            self._bar,
        ], spacing=2)

    def set_status(self, status: str, pct: float):
        labels = {"waiting": "等待", "running": "⏳ 处理中...", "done": "✓ 完成", "failed": "✗ 失败"}
        colors = {"waiting": TEXT_SECONDARY, "running": WARNING, "done": SUCCESS, "failed": "#d84040"}
        self._status.value = labels.get(status, status)
        self._status.color = colors.get(status, TEXT_SECONDARY)
        self._bar.content.width = pct
```

- [ ] **Step 2: 验证导入**

```bash
cd E:/text2story && python -c "from flet_ui.widgets.queue_view import QueueView; print('OK')"
```

- [ ] **Step 3: 提交**

```bash
git add flet_ui/widgets/queue_view.py
git commit -m "feat: 执行队列视图（每文件独立进度）"
```

---

### Task 7: 流水线页面

**Files:**
- Create: `flet_ui/pages/pipeline_page.py`

- [ ] **Step 1: 写 `flet_ui/pages/pipeline_page.py`**

```python
"""流水线 Tab：批量多文件 + 顺序/并行切换"""
import os
import asyncio
import flet as ft

from flet_ui.theme import BG_PRIMARY
from flet_ui.widgets.file_panel import FilePanel
from flet_ui.widgets.rules_bar import RulesBar
from flet_ui.widgets.queue_view import QueueView
from flet_ui.widgets.progress_bar import ProgressBar


class PipelinePage(ft.UserControl):
    """流水线页面"""

    def __init__(self, page: ft.Page):
        super().__init__()
        self.page = page
        self._progress = ProgressBar()
        self._queue = QueueView()
        self._rules = RulesBar(on_execute=self._on_execute)
        self._files = FilePanel(on_selection_change=self._on_files_changed)

    def build(self):
        return ft.Container(
            bgcolor=BG_PRIMARY,
            padding=0,
            content=ft.Column([
                ft.Row([
                    self._files,
                    ft.Column([
                        self._rules,
                        self._queue,
                    ], spacing=10, expand=True),
                ], spacing=10, expand=True),
                self._progress,
            ], spacing=10, expand=True),
            expand=True,
        )

    def _on_files_changed(self, selected: set[str]):
        names = [os.path.basename(p) for p in sorted(selected)]
        self._queue.set_files(names)

    def _on_execute(self):
        files = self._files.selected_files
        if not files:
            self._progress.log("⚠ 请先选择文件", "warn")
            return
        seq = self._rules.sequential
        rules = self._rules.rules
        self._progress.log(
            f"开始{'顺序' if seq else '并行'}执行 {len(files)} 个文件", "info"
        )
        self.page.run_task(self._run_pipeline(files, seq, rules))

    async def _run_pipeline(self, files: list[str], seq: bool, rules: dict[str, str]):
        """后台执行流水线"""
        import sys
        sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.dirname(__file__))))
        from config import Config
        from api.client_factory import create_clients

        try:
            bundle = create_clients()
            self._rules.set_api_connected(True)
        except ValueError as e:
            self._rules.set_api_connected(False)
            self._progress.log(f"✗ API 配置错误: {e}", "warn")
            return

        total = len(files)
        for idx, filepath in enumerate(files):
            name = os.path.basename(filepath)
            self._queue.set_status(name, "running", 0.0)
            self._progress.update_progress(f"[{name}] 阶段一 开始", 0.0)

            try:
                from core.srt_corrector import SrtCorrector
                from core.srt_converter import convert_srt_to_txt
                from core.storyboard_generator import StoryboardGenerator
                from utils.file_utils import read_file, write_file, get_output_dir_for_file, get_safe_stem

                stem = get_safe_stem(filepath, Config.INPUT_DIR)
                out_dir = get_output_dir_for_file(stem)
                os.makedirs(out_dir, exist_ok=True)

                self._progress.log(f"[{name}] SRT 修正...", "info")
                corrector = SrtCorrector(
                    client=bundle.client, model=bundle.model,
                    prompts_dir=Config.PROMPTS_DIR, max_chunk_size=Config.MAX_CHUNK_SIZE,
                )
                srt = read_file(filepath)
                corrected = await self._run_correction(corrector, srt, rules.get("srt_correction", "default"))
                corrected_path = os.path.join(out_dir, f"{stem}_corrected.srt")
                write_file(corrected_path, corrected)
                self._progress.update_progress(f"[{name}] SRT 修正 ✓", 0.25)
                self._progress.log(f"[{name}] SRT 修正完成", "success")

                self._progress.log(f"[{name}] 分镜生成...", "info")
                from core.srt_converter import convert_srt_to_txt
                txt = convert_srt_to_txt(corrected_path)
                txt_path = os.path.join(out_dir, f"{stem}_corrected.txt")
                write_file(txt_path, txt)

                generator = StoryboardGenerator(
                    client=bundle.client, model=bundle.model,
                    prompts_dir=Config.PROMPTS_DIR, max_chunk_size=Config.MAX_CHUNK_SIZE,
                )
                sb = await self._run_storyboard(generator, txt, rules.get("storyboard", "default"))
                sb_path = os.path.join(out_dir, f"{stem}_storyboard.txt")
                write_file(sb_path, sb)
                self._progress.update_progress(f"[{name}] 阶段一 完成 ✓", 1.0)
                self._progress.log(f"[{name}] 阶段一完成", "success")
                self._queue.set_status(name, "done", 1.0)

            except Exception as e:
                self._progress.log(f"[{name}] ✗ 失败: {e}", "warn")
                self._queue.set_status(name, "failed", 0.0)
                continue

            self._progress.update_progress(f"已完成 {idx+1}/{total}", (idx+1)/total)

        self._progress.log(f"全部完成 ({total} 文件)", "success")

    async def _run_correction(self, corrector, srt: str, prompt_name: str) -> str:
        results = []
        for event in corrector.iter_correct_with_progress(srt, prompt_name):
            if event.get("content"):
                results.append(event["content"])
        return "\n".join(results)

    async def _run_storyboard(self, generator, text: str, prompt_name: str) -> str:
        results = []
        for event in generator.iter_generate_progress(text, prompt_name):
            if event.get("content"):
                results.append(event["content"])
        return "\n".join(results)
```

- [ ] **Step 2: 验证导入**

```bash
cd E:/text2story && python -c "from flet_ui.pages.pipeline_page import PipelinePage; print('OK')"
```

- [ ] **Step 3: 提交**

```bash
git add flet_ui/pages/pipeline_page.py flet_ui/pages/__init__.py
git commit -m "feat: 流水线页面（批量多文件+顺序执行）"
```

---

### Task 8: 启动方式

**Files:**
- Create: `flet_ui/__main__.py`
- Modify: `main.py` (添加 `flet` 子命令)

- [ ] **Step 1: 写 `flet_ui/__main__.py`**

```python
"""python -m flet_ui"""
import flet as ft
from flet_ui.main import main

if __name__ == "__main__":
    ft.app(target=main)
```

- [ ] **Step 2: 修改 `main.py` 添加 `gui` 子命令**

在 `cli` 函数前新增 `gui` 命令：

```python
@cli.command()
def gui():
    """启动 Flet 桌面 GUI"""
    import flet as ft
    from flet_ui.main import main as flet_main
    ft.app(target=flet_main)
```

- [ ] **Step 3: 测试 CLI 入口**

```bash
cd E:/text2story && python main.py gui
```
Expected: Flet 窗口弹出，显示流水线 Tab

- [ ] **Step 4: 提交**

```bash
git add flet_ui/__main__.py main.py
git commit -m "feat: gui 子命令 + python -m flet_ui 入口"
```

---

## 验证清单

完成后逐项确认：

1. `python main.py gui` 弹出 1280x800 窗口，墨蓝黑配色
2. 顶部 6 个 Tab，流水线默认选中
3. 左侧文件面板可添加文件/目录，多选勾选
4. 规则栏四下拉同行排列，点击切换顺序/并行
5. 顺序模式按钮显示"顺序执行"，并行模式显示"批量执行"
6. 点击执行按钮，文件逐个进队列，底部日志滚动
7. 阶段一完成后输出文件到 `output/{stem}/` 目录
8. 其他 Tab 显示"开发中"占位文字
